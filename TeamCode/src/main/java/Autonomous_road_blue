package org.firstinspires.ftc.teamcode.drive;

import com.acmerobotics.roadrunner.geometry.Pose2d;
import com.qualcomm.robotcore.eventloop.opmode.Autonomous;
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;

@Autonomous(group = "drive")
public class Autonomous_goal_blue extends LinearOpMode {

    private SampleMecanumDrive drive;

    @Override
    public void runOpMode() throws InterruptedException
    {

        drive = new SampleMecanumDrive(hardwareMap);

        // מצב התחלה
        Pose2d startPose = new Pose2d(-63, 65, Math.toRadians(-90));
        drive.setPoseEstimate(startPose);
        double startY = startPose.getY();

        waitForStart();
        if (isStopRequested()) return;

        // MOVE 1 - 43 אינץ קדימה
        drive.followTrajectory(
                drive.trajectoryBuilder(drive.getPoseEstimate())
                        .back(43)
                        .build()
        );
        sleep(2000);

        // סיבוב  (לאיסוף)
        drive.turn(Math.toRadians(45));


        // MOVE 1 - 43 אינץ קדימה
        drive.followTrajectory(
                drive.trajectoryBuilder(drive.getPoseEstimate())
                        .strafeLeft(5)
                        .build()
        );


        // MOVE 1 - 43 אינץ קדימה
        drive.followTrajectory(
                drive.trajectoryBuilder(drive.getPoseEstimate())
                        .forward(30)
                        .build()
        );
        // MOVE 1 - 43 אינץ קדימה
        drive.followTrajectory(
                drive.trajectoryBuilder(drive.getPoseEstimate())
                        .back(32)
                        .build()
        );

        drive.turn(Math.toRadians(-47));


        drive.followTrajectory(
                drive.trajectoryBuilder(drive.getPoseEstimate())
                        .forward(9)
                        .build()
        );

        // 3 שניות "דמוי ירי" - להכניס כאן עוד ירי



        // שינוי זווית בסיום האוטונומי לכייל פילד אוריאנטד טלאופ .
        drive.update();  // עדכון נוסף לבטחון

        double finalY = drive.getPoseEstimate().getY();
        double finalX = drive.getPoseEstimate().getX();
        double finalDeg = drive.getRawExternalHeading();

        telemetry.addData("Start Y", "%.1f", startY);
        telemetry.addData("Final X", "%.1f", finalX);
        telemetry.addData("Final Y", "%.1f", finalY);
        telemetry.addData("Final Deg", "%.1f", Math.toDegrees(finalDeg));
        telemetry.update();

        sleep(3000);
    }
}
